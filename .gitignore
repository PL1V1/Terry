# Secrets — never commit
.env
.env.*
!.env.example
*.token
secrets/

# Data
data/
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm

# Dependencies / build
node_modules/
bun.lockb
dist/

# Logs
logs/
*.log

# OS
Thumbs.db
.DS_Store

# Instruction sources. The bodies live in the database, not the repo — these are
# your own content, and the brief is explicit that no instruction catalogue gets
# committed here.
instructions/
